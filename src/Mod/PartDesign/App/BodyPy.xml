<?xml version="1.0" encoding="UTF-8"?>
<GenerateModel xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="generateMetaModel_Module.xsd">
  <PythonExport 
    Father="BodyBasePy"  
    Name="BodyPy"
    Twin="Body" 
    TwinPointer="Body" 
    Include="Mod/PartDesign/App/Body.h" 
    Namespace="PartDesign" 
    FatherInclude="Mod/Part/App/BodyBasePy.h" 
    FatherNamespace="Part">
    <Documentation>
      <Author Licence="LGPL" Name="Juergen Riegel" EMail="FreeCAD@juergen-riegel.net" />
      <UserDocu>PartDesign body class</UserDocu>
    </Documentation>
    <Methode Name="addFeature">
            <Documentation>
                    <UserDocu>addFeature(feat) - Add the given feature after the current Tip feature</UserDocu>
            </Documentation>
    </Methode>
    <Methode Name="removeFeature">
            <Documentation>
                    <UserDocu>removeFeature(feat) - Remove the given feature from the Body</UserDocu>
            </Documentation>
    </Methode>
    <Methode Name="insertFeatureAfter">
            <Documentation>
                    <UserDocu>insertFeatureAfter(feature, after)
                        Insert the feature into the body after the given feature.
                        @param feature  The feature to insert into the body
                        @param after    The feature after the given one should be inserted.
                                        If after is None than insert the feature as first.
                    </UserDocu>
            </Documentation>
    </Methode>
    <Methode Name="insertFeatureBefore">
            <Documentation>
                    <UserDocu>insertFeatureBefore(feature, before)
                        Insert the feature into the body before the given feature.
                        @param feature  The feature to insert into the body
                        @param before   The feature before the given one should be inserted.
                                        If before is None than insert the feature as last.
                        Note: the methode doesn't modifies the Tip
                    </UserDocu>
            </Documentation>
    </Methode>
  </PythonExport>
</GenerateModel>
